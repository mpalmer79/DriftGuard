# DriftGuard backend (uvicorn / FastAPI).
# No required runtime env vars; SQLite is in-memory by default.
# Persistence, auth, CORS, rate limits, tracing, and trusted-proxy
# config are all opt-in via SENTINEL_* env vars (legacy prefix, kept
# for deployment compatibility — see docs/DEPLOYMENT.md).
PYTHONUNBUFFERED=1
